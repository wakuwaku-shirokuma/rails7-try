<p style="color: green"><%= notice %></p>

<h1>企業一覧</h1>

<div id="companies">
  <% @companies.each do |company| %>
    <%= render company %>
    <p>
      <%= link_to "Show this company", company %>
    </p>
  <% end %>
</div>

<%= link_to "企業作成", new_company_path %>
